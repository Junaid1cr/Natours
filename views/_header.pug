header.header 
  nav.nav.nav--tours 
    a.nav__el(href='/') All tours 
  .header__logo 
    img(src='/img/logo-while.png' alt='Natours logo')

  nav.nav.nav--user 
    if user
      a.nav__el.nav__el--logout Log Out
      a.nav__el(href='/me')
        img(src=`img/users/${user.photo}` alt='Natours logo' class='image')
        span= `${user.name.split(' ')[0]}`
    else
      a.nav__el(href='/login') Log in 
      button.nav__el.nav__el--cta Sing Up
